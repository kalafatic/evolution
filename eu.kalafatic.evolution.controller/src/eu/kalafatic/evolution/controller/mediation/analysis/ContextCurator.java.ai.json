{
  "domain": "evolution",
  "purpose": "",
  "role": "mediation",
  "stability": "STABLE",
  "mediatedRelevanceScore": 1.0,
  "importanceScore": 1.0,
  "summary": "package eu.kalafatic.evolution.controller.mediation.analysis; import java.util.ArrayList; import java.util.HashSet; import java.util.List; import java.util.Set; import java.util.stream.Collectors; import eu.kalafatic.evolution.controller.mediation.model.FileDescriptor; import eu.kalafatic.evolution.controller.mediation.model.SemanticEdge; import eu.kalafatic.evolution.controller.mediation.model.SemanticNode; import eu.kalafatic.evolution.controller.mediation.model.TargetDescriptor; import eu.kalafatic.evolution.controller.mediation.model.TargetSnapshot; public class ContextCurator { public List<String> curate(TargetDescriptor target) { List<String> curatedPaths = new ArrayList<>(); curatedPaths.addAll(target.getFiles().stream() .filter(f -> f.getTags().contains(\"Entry Point\")) .map(f -> f.getPath()) .collect(Collectors.toList())); curatedPaths.addAll(target.getFiles().stream() .filter(f -> f.getPath().endsWith(\"pom.xml\") || f.getPath().endsWith(\"package.json\"))",
  "evolutionaryNotes": "Suggestion: Add @EvolutionComponent annotation to this high-importance class.",
  "contextSelectionHints": ["mandatory_context", "mandatory_context", "mandatory_context", "mandatory_context", "mandatory_context"],
  "dependencyLinks": []
}