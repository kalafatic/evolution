{
  "domain": "evolution",
  "purpose": "",
  "role": "orchestration",
  "stability": "STABLE",
  "mediatedRelevanceScore": 1.0,
  "importanceScore": 1.0,
  "summary": "package eu.kalafatic.evolution.controller.orchestration.selfdev; import java.util.Comparator; import java.util.List; import org.json.JSONArray; import org.json.JSONObject; public class DarwinFitnessRanker { public void rank(List<JSONObject> variants) { rank(variants, false, 0); } public void rank(List<JSONObject> variants, boolean isAtomicRound) { rank(variants, isAtomicRound, 0); } public void rank(List<JSONObject> variants, boolean isAtomicRound, int generation) { for (JSONObject v : variants) { double score = calculateFitness(v, generation); if (isAtomicRound && DarwinStrategyType.PROBABLE_SURVIVOR.name().equals(v.optString(\"strategy_type\"))) { score = Math.max(score, 0.95); } v.put(\"score\", score); }",
  "evolutionaryNotes": "Suggestion: Add @EvolutionComponent annotation to this high-importance class.",
  "contextSelectionHints": ["mandatory_context"],
  "dependencyLinks": []
}