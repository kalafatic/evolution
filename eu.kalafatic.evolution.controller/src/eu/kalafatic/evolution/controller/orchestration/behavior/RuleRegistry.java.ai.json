{
  "domain": "evolution",
  "purpose": "",
  "role": "orchestration",
  "stability": "STABLE",
  "mediatedRelevanceScore": 1.0,
  "importanceScore": 1.0,
  "summary": "package eu.kalafatic.evolution.controller.orchestration.behavior; import java.util.ArrayList; import java.util.List; public class RuleRegistry { private static final List<PolicyRule> rules = new ArrayList<>(); static { rules.add(policy -> { if (policy.getExecutionMode() == ExecutionPolicy.ExecutionMode.MEDIATED && policy.getReasoningStrategy() == ExecutionPolicy.ReasoningStrategy.DARWIN) { policy.setSupervisionLevel(ExecutionPolicy.SupervisionLevel.HYBRID); policy.setInteractionMode(ExecutionPolicy.InteractionMode.STEP); policy.addConstraint(\"MEDIATED_DARWIN_GUARD: Human must review all mutation variants.\"); } }); rules.add(policy -> { if (policy.getExecutionMode() == ExecutionPolicy.ExecutionMode.LOCAL) { policy.setRepositoryMode(ExecutionPolicy.RepositoryMode.ISOLATED); } }); rules.add(policy -> {",
  "evolutionaryNotes": "Suggestion: Add @EvolutionComponent annotation to this high-importance class.",
  "contextSelectionHints": ["mandatory_context", "mandatory_context", "mandatory_context", "mandatory_context", "mandatory_context"],
  "dependencyLinks": []
}