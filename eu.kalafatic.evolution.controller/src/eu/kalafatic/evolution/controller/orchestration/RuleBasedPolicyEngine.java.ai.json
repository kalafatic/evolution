{
  "domain": "evolution",
  "purpose": "",
  "role": "orchestration",
  "stability": "STABLE",
  "mediatedRelevanceScore": 1.0,
  "importanceScore": 1.0,
  "summary": "package eu.kalafatic.evolution.controller.orchestration; import org.json.JSONObject; import eu.kalafatic.evolution.controller.orchestration.llm.LlmRouter; public class RuleBasedPolicyEngine implements IPolicyEngine { private static final double CONFIDENCE_THRESHOLD = 0.6; private final LlmRouter llmRouter = new LlmRouter(); @Override public String evaluate(JSONObject classification, String input, TaskContext context) throws Exception { String intent = classification.optString(\"intent\", \"\").trim().toLowerCase(); String category = classification.optString(\"category\", \"\").trim().toUpperCase(); double confidence = classification.optDouble(\"confidence\", 0.0); boolean needsClarification = classification.optBoolean(\"needs_clarification\", false); if (intent.isEmpty() || \"unclear\".equals(intent)) { if (\"CODING\".equals(category) || \"TOOL_USE\".equals(category) || \"RESEARCH\".equals(category)) { intent = \"new\"; confidence = Math.max(confidence, 0.9); } else if (\"CHAT\".equals(category)) { intent = \"chat\"; confidence = Math.max(confidence, 0.9); }",
  "evolutionaryNotes": "Suggestion: Add @EvolutionComponent annotation to this high-importance class.",
  "contextSelectionHints": ["mandatory_context", "mandatory_context", "mandatory_context", "mandatory_context", "mandatory_context"],
  "dependencyLinks": []
}