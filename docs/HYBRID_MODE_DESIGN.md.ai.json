{
  "domain": "evolution",
  "purpose": "",
  "role": "documentation",
  "stability": "",
  "mediatedRelevanceScore": 0.5,
  "importanceScore": 0.5,
  "summary": "Large Language Models (LLMs) running locally (e.g., via Ollama) are excellent for privacy and cost-efficiency. However, models that fit on consumer hardware (7B to 14B parameters) often struggle with: The Evolution Platform implements a **Hybrid Mode** that decouples \"Context Gathering\" from \"Deep Reasoning.\" 1.  The local model performs a deterministic scan of the project structure. 2.  It selects only the most relevant files, classes, and method signatures related to the current task. 3.  It builds a structured `ContextPackage` (JSON) containing minimal but high-density information. 1.  The structured `ContextPackage` is sent to a powerful remote model. 2.  The remote model, unburdened by irrelevant noise, performs high-precision reasoning and generates the implementation or plan. 1.  The result from the cloud is returned to the local environment. 2.  The local model performs a quick safety and format check (Verification). 3.  It ensures the response aligns with local architectural guardrails before application. By using the local model as a \"Librarian\" to filter and structure information, we feed the cloud model a \"Technical Brief\" rather than a \"Dump of Files.\" This hybrid synergy allows the system to achieve results that neither a small local model nor a \"blind\" cloud model could achieve alone. The `LlmRouter` is designed with **Automatic Resilient Fallback**. If Phase 2 (Cloud Reasoning) fails due to network issues or API limits, the system automatically attempts to complete the task using the local model in `LOCAL` mode, ensuring that the development workflow is never fully blocked.",
  "evolutionaryNotes": "",
  "contextSelectionHints": [],
  "dependencyLinks": []
}