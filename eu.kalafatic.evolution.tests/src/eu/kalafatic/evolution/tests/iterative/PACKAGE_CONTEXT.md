# PACKAGE CONTEXT

## Directory: eu.kalafatic.evolution.tests/src/eu/kalafatic/evolution/tests/iterative/

## Domain: general

## Components
* `AutonomousImprovementTest.java`: package eu.kalafatic.evolution.tests.iterative; import java.util.Random; public class AutonomousImprovementTest implements ISimulationTest { private final ITestListener listener; private final Random random = new Random(); private boolean stop = false; public AutonomousImprovementTest(ITestListener listener) { this.listener = listener; } @Override public void run() { stop = false; listener.reset(); executeStep("observe", 0); } @Override public void stop() { this.stop = true; } private void executeStep(String step, int iterationCount) {
* `ITestListener.java`: package eu.kalafatic.evolution.tests.iterative; public interface ITestListener { void stepStarted(String step); void stepSuccess(String step); void stepFailed(String step); void stepSkipped(String step); void transitionActive(String edgeId); void reset(); }
* `ISimulationTest.java`: package eu.kalafatic.evolution.tests.iterative; public interface ISimulationTest { void run(); void stop(); }
* `ProjectSetupTest.java`: package eu.kalafatic.evolution.tests.iterative; import java.util.Random; public class ProjectSetupTest implements ISimulationTest { private final ITestListener listener; private final Random random = new Random(); private boolean stop = false; public ProjectSetupTest(ITestListener listener) { this.listener = listener; } @Override public void run() { stop = false; listener.reset(); executeStep("observe"); } @Override public void stop() { this.stop = true; } private void executeStep(String step) {
* `ManualOrchestrationTest.java`: package eu.kalafatic.evolution.tests.iterative; import java.util.Random; public class ManualOrchestrationTest implements ISimulationTest { private final ITestListener listener; private final Random random = new Random(); private boolean stop = false; public ManualOrchestrationTest(ITestListener listener) { this.listener = listener; } @Override public void run() { stop = false; listener.reset(); executeStep("observe"); } @Override public void stop() { this.stop = true; } private void executeStep(String step) {
* `IterativeDevelopmentTest.java`: package eu.kalafatic.evolution.tests.iterative; import java.util.Random; public class IterativeDevelopmentTest implements ISimulationTest { private final ITestListener listener; private final Random random = new Random(); private boolean stop = false; public IterativeDevelopmentTest(ITestListener listener) { this.listener = listener; } @Override public void run() { stop = false; listener.reset(); executeStep("observe", 0); } @Override public void stop() { this.stop = true; } private void executeStep(String step, int iterationCount) {
