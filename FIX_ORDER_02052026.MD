# Implementation Roadmap: Hardened Evolutionary Kernel

## Executive Summary
You are correct. Fixing the **Self-Dev + Iterative + Darwin** scenario (the most complex path) effectively builds the architectural "superset" for the entire system. Once the structural constraints required for this scenario are enforced, all other modes become deterministic subsets of this core logic.

---

## implementation Strategy: The "Superset-First" Approach

### PHASE 1: The Evolutionary Kernel (The Model)
**Target:** `eu.kalafatic.evolution.model`
Before fixing logic, we must define the "Contract".
- **Global State Authority:** Define `SystemState` (INIT, ANALYZING, PLAN_LOCKED, EXECUTING, VERIFYING, DONE).
- **Immutable Plan:** Add a `finalPlan` field to the `Task` and `Iteration` models.
- **Why:** This creates the structural "teeth" that ChatGPT identified as missing.

### PHASE 2: The Sole Transition Authority (IterationManager)
**Target:** `IterationManager.java`
Implement the "Single Transition Engine" for the most complex scenario.
- Port the Self-Dev + Darwin logic to be the **only** place where `SystemState` can advance.
- Ensure that `DarwinEngine` and `TaskPlanner` outputs are treated as raw data signals, consumed solely by this layer.
- **Why:** This eliminates the "Authority Chain" problem by centralizing all decision-making.

### PHASE 3: The Blind Executor (EvolutionOrchestrator)
**Target:** `EvolutionOrchestrator.java`
Strip the Orchestrator of its tactical autonomy.
- Add a mandatory check: `IF state == EXECUTING && hasFinalPlan → executeBlindly()`.
- Explicitly bypass `intentClassifier` and `analyticAgent` in this mode.
- **Why:** This solves "Redundant Analysis" and "Triple-Layered Planning" permanently.

### PHASE 4: Convergence (Entry Point Unification)
**Target:** `AiChatPage.java`, `SelfDevSupervisor.java`
Unify all system entry points.
- Refactor the Chat UI and Supervisor to delegate **all** iterative or Darwinian requests to the newly hardened `IterationManager`.
- **Why:** This ensures "Logic Unification" and prevents functional drift between different UI tabs.

---

## Conclusion: Is the Assumption Correct?
**Yes.**

The **Self-Dev + Iterative + Darwin** flow is the only one that exercises every architectural layer (Supervisor -> Planner -> DarwinEngine -> Orchestrator -> Tool).

By fixing this scenario first:
1. You build the **Global State Machine** (needed by all).
2. You establish the **Trust Boundary** (protecting the Orchestrator).
3. You define the **Data Contract** (decoupling intelligence from control).

The Simple Chat and Standard Iteration modes then simply become "short-circuit" paths through this same robust kernel, inheriting the stability and performance gains of the complex fix.
